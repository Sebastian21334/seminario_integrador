// Entorno de producción: reemplazar por la URL del backend desplegado en Azure
// (App Service / Container Apps, etc.). Se resuelve vía fileReplacements en angular.json.
export const environment = {
  production: true,
  apiUrl: 'https://TU-BACKEND-EN-AZURE.azurewebsites.net',
};
